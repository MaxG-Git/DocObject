import { ListGroup, ListItem } from "./bootstrap/ListGroup"
import { Pagination, PageItem } from "./bootstrap/Pagination"
import { Dial } from "./vanilla/Dial"


export const Bootstrap = {
    ListGroup, 
    ListItem,
    Pagination,
    PageItem
}

export const Vanilla = {
    Dial
};